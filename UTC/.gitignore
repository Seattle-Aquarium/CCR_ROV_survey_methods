# build artefacts -- the packaged exe is ~300 MB, never commit it
build/
dist/
*.spec.bak
__pycache__/
*.pyc

# per-run scratch
.pytest_cache/

# test artefacts -- the visual scripts write PNGs next to themselves
gui_dark.png
gui_light.png
tests/*.png
tests/out/
*.part

# packaging / tooling
*.egg-info/
.ruff_cache/

# RAW develop test imagery -- inputs and exports, ~420 MB. The live script
# takes a folder argument, so nothing here needs to be in the repo.
tests/GPR/
tests/TIF/
